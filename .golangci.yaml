# Defines the configuration version.
# The only possible value is "2".
version: "2"

linters:
  # Start from the standard set; add the handful of extras that catch real
  # bugs (race-prone patterns, unchecked errors, context misuse) without
  # forcing style debates.
  default: standard
  enable:
    - bodyclose
    - errcheck
    - errorlint
    - gosec
    - ineffassign
    - misspell
    - unconvert
    - unused

  exclusions:
    paths:
      - "tools/gen-config"
    rules:
      # Tests routinely ignore errors and use net/http test helpers.
      - path: _test\.go
        linters: [errcheck, gosec, bodyclose]

formatters:
  # Enable specific formatter.
  # Default: [] (uses standard Go formatting)
  enable: []

# Options for analysis running.
run:
  # Timeout for total work, e.g. 30s, 5m, 5m30s.
  # If the value is lower or equal to 0, the timeout is disabled.
  # Default: 0 (disabled)
  timeout: 5m

  # The mode used to evaluate relative paths.
  # It's used by exclusions, Go plugins, and some linters.
  # The value can be:
  # - `gomod`: the paths will be relative to the directory of the `go.mod` file.
  # - `gitroot`: the paths will be relative to the git root (the parent directory of `.git`).
  # - `cfg`: the paths will be relative to the configuration file.
  # - `wd` (NOT recommended): the paths will be relative to the place where golangci-lint is run.
  # Default: cfg
  relative-path-mode: cfg

  # Exit code when at least one issue was found.
  # Default: 1
  issues-exit-code: 1

  # Include test files or not.
  # Default: true
  tests: true

  # List of build tags, all linters use it.
  # Default: []
  build-tags:
    - unit
